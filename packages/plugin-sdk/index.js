/**
 * Returns a plugin definition unchanged for JavaScript plugin packages.
 *
 * @param {unknown} plugin Plugin definition exported by a package.
 * @returns {unknown} The same plugin definition.
 */
export function definePlugin(plugin) {
  return plugin;
}
