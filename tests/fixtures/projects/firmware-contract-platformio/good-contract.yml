version: 1
projects:
  - path: .
    pinmap: pins.yml
    firmware:
      platformio:
        pinAssignments: platformio-pins-good.yml
rules:
  drc.kicad:
    enabled: false
  erc.kicad:
    enabled: false
fail-on: high
