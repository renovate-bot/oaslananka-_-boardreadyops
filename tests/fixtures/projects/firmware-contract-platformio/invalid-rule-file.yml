version: 1
projects:
  - path: .
    pinmap: pins.yml
rules:
  firmware.platformio-pin-contract:
    file: invalid-contract.yml
  drc.kicad:
    enabled: false
  erc.kicad:
    enabled: false
fail-on: high
