version: 1
projects:
  - path: .
    pinmap: pins.yml
rules:
  firmware.zephyr-pin-contract:
    file: invalid-contract.yml
  drc.kicad:
    enabled: false
  erc.kicad:
    enabled: false
  release.changelog-present:
    enabled: false
fail-on: never
